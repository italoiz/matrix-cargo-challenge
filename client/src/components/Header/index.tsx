import React from 'react';

import './styles.scss';

export const Header: React.FC = () => (
  <header className="app-header">
    <h1>Repository Explorer</h1>
    <p>Choose an language to search repositories on the Github platform.</p>
  </header>
);
